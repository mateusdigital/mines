{
    "configurations": [
        {
            "name": "Launch",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/scripts/start-emu.ps1",
            "cwd": "${cwd}",
            "preLaunchTask": "Build"
        }
    ]
}