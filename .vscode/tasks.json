{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "${workspaceFolder}/scripts/build-game.ps1",
            "group": {
                "kind": "build",
                "isDefault": true
            },

        }
    ]
}
